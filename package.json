{
  "name": "rooms-aaa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n 'Rooms-oussama,Server' -c 'red,green' \"cd Server && npm start && cd-\" \"cd Rooms-oussama && npm start && cd-\"",
    "build": "cd Rooms-oussama && npm run build",
    "install-front": "cd Rooms-oussama && npm install",
    "front": "cd Rooms-oussama && npm start",
    "heroku-postbuild": "npm run install-front && npm run build",
    "start": "cd Server && node index.js && cd-"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "framer-motion": "^6.3.3",
    "react-dropdown": "^1.10.0",
    "react-modal": "^3.15.1"
  },
  "devDependencies": {
    "concurrently": "^7.2.1"
  }
}
