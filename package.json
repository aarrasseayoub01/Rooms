{
  "name": "rooms-aaa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "v16.15.0",
    "npm": "8.5.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd Rooms-oussama && npm run build",
    "install-front": "cd Rooms-oussama && npm install",
    "install-back": "cd Server && npm install",
    "heroku-postbuild": "npm run install-back && npm run install-front && npm run build",
    "dev": "cd Server && node server.js && cd-",
    "front": "cd Rooms-oussama && npm start",
    "start": "concurrently -n 'Rooms-oussama,Server' -c 'red,green' \"cd Server && npm start && cd-\" \"cd Rooms-oussama && npm start && cd-\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "framer-motion": "^6.3.3",
    "react-dropdown": "^1.10.0",
    "react-modal": "^3.15.1"
  },
  "devDependencies": {
    "concurrently": "^7.2.1"
  }
}
